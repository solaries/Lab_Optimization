@model IEnumerable<Lo.Models.Lo_Tests_data>
 
 
<div class="card">
    <div class="card-body">
        <h4 class="card-title">View Patient Tests</h4> 
        <div class="table-responsive">
            <table class="table table-striped table-bordered zero-configuration">
                <thead>
                    <tr>
                        <th>   </th> 
                        <th>patient</th>
                        <th>Test Date</th>
 
                    </tr> 
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>                            
                            <td>@Html.ActionLink("Select", "edit_Patient Tests", new
                           {
                            id=item.Id, 
                            Test_Date = item.Test_date
                            ,patient = item.Patient
                               })</td> 
                           <td>@Html.DisplayFor(modelItem => item.Patient_data)</td>
                           <td>@Html.DisplayFor(modelItem => item.Test_date)</td>
                        </tr>
                    }
                    
                </tbody>
                <tfoot>
                    <tr>                        
                        <th>   </th> 
                        <th>patient</th>
                        <th>Test Date</th>
 
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div> 
<script> 
    function msg(txt) {
        sweetAlert("...", txt, "info");
    }
    var statusMessage = "@ViewBag.status" || false; 
    if (statusMessage != false && statusMessage.length > 0) {
        msg(statusMessage); 
    }
</script>